#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

void init(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
}

int main(){
	init();
	int n,k,cnt=0;
	int *a = new int[n];
	int i,j,k;

	for(i=0;i<n;i++){
		cin>>a[i];
	}
	
	return 0;
}

#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using namespace std;

void init(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
}

int dp[300];

int step(int s1, int s2){

}

int main(){
	init();
	int n,i,ans=0;
	cin>>n;
	vector<int> v(n);
	for(i=0;i<n;i++){
		cin>>v[i];
	}
	
	i=0;
	while(true){
		
	}
	return 0;
}

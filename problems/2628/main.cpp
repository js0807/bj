#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using namespace std;

void init(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
}

int main(){
	init();
	int n,m;
	cin>>n>>m;
	int a[n][m];
	int t;
	cin>>t;
	for(int i=0;i<t;i++){
		int l,num;
		cin>>l>>num;
		if(l==0){
			
		} else{

		}
	}
	return 0;
}

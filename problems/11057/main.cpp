#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using namespace std;

void init(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
}

int main(){
	init();
	int n;
	long long dp[1001]={0,10,55,220,};
	cin>>n;
	for(int i=3;i<=n;i++){
		
	}
	return 0;
}
